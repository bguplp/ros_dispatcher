To see a simulation:
1. run these commands:
roscore
rosrun ros_dispatcher move_to_point_server.py
rosrun ros_dispatcher observe_can_server.py
rosrun ros_dispatcher pick_server.py 
rosrun ros_dispatcher launcher.py
2. run the planner
3. look at the luancher.py terminal
